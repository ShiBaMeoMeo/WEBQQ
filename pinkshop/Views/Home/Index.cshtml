@{
    ViewData["Title"] = "Home Page";
}
@model List<pinkshop.Models.Product>

<h2>Our Products</h2>

    @foreach(var p in Model){
     // partial hiện ra một sản phảm
     // Lặp lại cái partial để hiện ra nhiều sản phẩm bằng foreach với model p
    <partial name="_Product" model="p" />

    @* <div class="product_box margin_r35">

        <h3>@p.Name</h3>

        <div class="image_wrapper">  <a asp-action="Detail" asp-controller="Product" asp-route-id="@p.Id"><img src="~/@p.Image" alt="product 2" /></a> </div>

        <p class="price">Price: $@p.Price</p>
        <a asp-action="Detail" asp-controller="Product" asp-route-id="@p.Id">Detail</a> |
        <a asp-route="addcart" asp-route-productid="@p.Id">Buy Now</a>
    </div> *@

    }


<div class="cleaner"></div>

<div class="button_01"><a href="#">View All</a></div>


    